---
milestone: v3.6
milestone_name: 端到端测试体系与网络隔离验证
audited_at: 2026-05-14
auditor: Claude (gsd-audit-milestone)
status: tech_debt
phases_complete: 8/8
plans_complete: 39/39
requirements_total: 38
requirements_satisfied: 38
requirements_deferred: 0
human_verification_pending: 9
tech_debt_items: 8
gap_closures_within_milestone: 3
new_dependencies: 1
ship_recommendation: ship_with_followups
---

# v3.6 端到端测试体系与网络隔离验证 — Milestone Audit

**Audited:** 2026-05-14
**Scope:** Phases 45 / 46 / 47 / 48 / 49 / 50 / 51 / 52（8 phase / 39 plan 全部 ship-ready）

---

## 1. 执行摘要

本里程碑沿着 `.planning/REQUIREMENTS.md` 锁定的 38 条 v1 需求（E2E-01..05 / MVS-01..10 / LEAK-01..08 / KILL-01..04 / QUAL-01..08 / OBS-01..03）一次性长出 cloud-cli-proxy 自己的端到端测试体系：`tests/e2e/` 目录下接入 testcontainers-go + testify/suite + Scenario 声明式拓扑 + `waitFor` 条件等待 + ArtifactDumper 失败归档；CI 走 hosted ubuntu-24.04 双层 workflow（lint + e2e job + paths 守护 + 失败 PR 评论）；MVS / LEAK / KILL 三套真实跑路径的 e2e 用例骨架就位；Phase 51 把 9 项代码层质量加固（verify 多源轮询 / 多目标泄漏检测 / 全 nameserver 校验 / namespace 探测窗口 option / nft 全规则 counter + 显式 link-local drop / worker cap-drop NET_RAW / `-race -shuffle=on` 默认 / goleak.VerifyTestMain / 双绑 pre-check 收口）一次性落地；Phase 52 把失败诊断做成一键工程（`collect-artifacts.sh` + 5 子目录 README 模板 + `if: failure()` upload-artifact@v4）。

**darwin 维度全部通过的核心证据：**`go build ./...` × `GOOS=linux go build ./...` × `GOOS=linux go build -tags='e2e linux' ./tests/e2e/...` × `go test ./... -race -shuffle=on -count=1` × `go vet ./...` × `bash scripts/lint-no-bare-sleep.sh` 六道闸全绿；累计纯函数单测 darwin 直跑 121 PASS（Phase 45 14 + Phase 46 24 + Phase 47 17 + Phase 48 20 + Phase 49 32 + Phase 50 29 + Phase 51 32 + Phase 52 8 - 共享去重 ≈ 121 PASS）；harness 自身 23 个单测全绿；`scripts/lint-no-bare-sleep.sh` 双层守护（ci.yml 永跑 + e2e.yml paths 强制）确认 `tests/e2e/` 内零裸 `time.Sleep`。

**Linux 真机维度 deferred-to-CI 项数量：**9 个签字点（含 hosted ubuntu-24.04 runner 上的 MVS-06/07/08 / MVS-09/10 / LEAK-01..08 / KILL-01..04 / VERIFY-1 artifact 上传 / VERIFY-2 PR 评论），全部按 CONTEXT §Area 4 锁定的「darwin 编译 + 纯函数单测 PASS = passed；Linux 真机断言 deferred-to-CI 非阻塞 ship」标准列入 `human_verification`，不构成阻塞。Linux runner 跑通的前置仍是 Phase 45 Plan 02 Scenario.Start Step 2..7 真实接入（控制面子进程 / admin login / fixture 三件套 / PrepareGateway / PrepareHost），属下一里程碑的 P1 收尾工作。

**backend GAP 是否在本里程碑内闭环：**Phase 49 初次验证以 `gaps_found` 结案（LEAK-06 raw socket、LEAK-07 link-local 显式 drop、LEAK-08 capability 审计），3 条 backend gap 在 Phase 51 同里程碑内闭环：QUAL-06 删 `--cap-add SYS_ADMIN` + 显式 `--cap-drop NET_RAW`（NET_ADMIN 按 sing-box tun 依赖保留，CONTEXT §Area 4 允许的折中），QUAL-05 在 worker 输出链注入 `ip daddr 169.254.0.0/16 counter drop comment "linklocal-drop"`，所有 nft 规则插入 `expr.Counter{}` 表达式。同时 Phase 47 D-47-3「双绑互斥后端缺 pre-check」在 51-09 一次性收口（admin_bindings.go 新增 `ErrCodeEgressIPAlreadyBound` 常量 + Bind pre-check + 409 + 中英双语 message + host_id / egress_ip_id 字段回显），完全闭环；Phase 47 darwin 纯函数单测 `TestHelpersParseBindEgressIPResponse_*` 自动从 PARTIAL → PASS。

**是否引入新依赖：****仅 1 条**：`go.uber.org/goleak v1.3.0`（QUAL-08 goleak.VerifyTestMain 接入，唯一允许的新生产依赖；ignore list 仅 `broadcast.(*Hub).cleanupLoop` 设计内常驻 goroutine）。前端零改动；前端构建工具链零改动；Docker / Postgres / OpenSSH / sing-box / Node / Go 版本均不变。

---

## 2. 需求覆盖矩阵

| Category | 需求 ID | 覆盖 Phase / Plan | 主用例 / 证据 | 状态 |
|----------|---------|-------------------|---------------|------|
| **E2E** | E2E-01 | 45 / 45-01 | `tests/e2e/{doc.go,suite_test.go}` + `tests/e2e/harness/suite.go` + testcontainers-go + testify/suite + `go test -tags=e2e ./tests/e2e/...` PASS | ✓ |
| | E2E-02 | 45 / 45-02 | `harness.Scenario` builder API：`.WithControlPlane().WithSingBoxGateway(...).WithHost(...).WithUser(...)`；Step 1 真实 + Step 2..7 TODO（partial，骨架就位） | ✓（骨架，Step 2..7 待下游真实接入） |
| | E2E-03 | 45 / 45-05 | `.github/workflows/e2e.yml` 双 job（lint + e2e）；CONTEXT §Area 3 调整为 hosted ubuntu-24.04（自管 runner 弃用） | ✓ |
| | E2E-04 | 45 / 45-04 | `tests/e2e/harness/artifacts.go` `ArtifactDumper` + 5 子目录（logs/network/docker/postgres/system）+ `actions/upload-artifact@v4` retention 30 天 | ✓ |
| | E2E-05 | 45 / 45-03 + 45-05 | `harness.WaitFor` + 4 helper（Log/Port/HTTP/Exec）8/8 单测 + `scripts/lint-no-bare-sleep.sh` 双层守护 | ✓ |
| **MVS** | MVS-01 | 46 / 46-01 | `TestBootstrap_GoldenPath` + `GoldenPath`/`StartGoldenPath` + 24 个纯函数单测；events.host.ready 双重确认 deferred-to-CI | ✓（骨架） |
| | MVS-02 | 46 / 46-02 | `EgressIPSources()` 三源（ip.me / ifconfig.io / ipinfo.io）+ `Vote` 多数派裁决 6 分支单测 PASS；公网 IP 等值断言 deferred-to-CI | ✓ |
| | MVS-03 | 46 / 46-03 | `ClassifyDNSResult` Tunneled/Denied OR 语义；nft counter dump deferred-to-OBS | ✓ |
| | MVS-04 | 46 / 46-04 | `DefaultDenyMatrix` 4 target × 3s timeout × 并发 + `BuildDenyProbeCmd` + `SummarizeDenyResults` | ✓ |
| | MVS-05 | 46 / 46-05 | `BootstrapExitCodeContract` 与 `internal/controlplane/http.BootstrapErrorEntries` 编译期 cross-check；被测 binary 改为 `deploy/bootstrap/cloud-bootstrap.sh`（ROADMAP 偏差，CONTEXT §Area 3 以源码为准） | ✓ |
| | MVS-06 | 47 / 47-01 | `TestExpiry_AutoStop_GoldenPath` + `ExpiryEventType="host.stop.expired"` 锁定常量；`cmd/control-plane/main.go` 新增 `EXPIRY_SCAN_INTERVAL` 环境变量解析 | ✓ |
| | MVS-07 | 47 / 47-02 → 51 / 51-09 | `TestEgressIPBinding_DoubleBindExcluded` + `EgressIPDoubleBindContract{WantStatus:409, WantErrSubstring:"already bound"}`；51-09 闭 backend gap | ✓（gap 闭环） |
| | MVS-08 | 47 / 47-03 | `TestHostAgent_KillRecover_GoldenPath` + `HostHealthStatus` 枚举 + `HostHealthRecoveryContract{UnhealthyWithin:30s, HealthyWithin:60s}` | ✓ |
| | MVS-09 | 48 / 48-01 | `TestKillSwitch_SingboxCrash_GoldenPath` + `ClassifyKillswitchResult` + `KillswitchTimingContract{ProbeMaxLatency:3s, TcpdumpWindow:5s}` + privileged sidecar tcpdump 路径 | ✓ |
| | MVS-10 | 48 / 48-02 | `TestKillSwitch_ResolvConfTamper_GoldenPath` + `ClassifyResolvConfDNSOutcome` Tunneled/Denied OR 6 分支表 | ✓ |
| **LEAK** | LEAK-01 | 49 / 49-01 | `TestLeak_01_DNSPlainUDP_BlockedByHostEth0` + `ClassifyLeakProbe` × 5 fixture | ✓ |
| | LEAK-02 | 49 / 49-02 | `TestLeak_02_DoT853_BlockedByHostEth0` + `kdig +tls` / `openssl s_client` 双向探测 | ✓ |
| | LEAK-03 | 49 / 49-03 | `TestLeak_03_ICMP_BlockedByHostEth0` + ping perm_denied / unreachable / ok 三 fixture | ✓ |
| | LEAK-04 | 49 / 49-04 | `TestLeak_04_IPv6_BlockedByHostEth0` + `disable_ipv6=1` 双保险 + 条件性 ip6 抓包 | ✓ |
| | LEAK-05 | 49 / 49-05 | `TestLeak_05_IMDS_BlockedByHostEth0` 覆盖 169.254.169.254 + 169.254.170.2 | ✓ |
| | LEAK-06 | 49 / 49-06 → 51 / 51-06 | `TestLeak_06_RawSocket_PermissionDenied`；51-06 显式 `--cap-drop NET_RAW` 闭 gap | ✓（gap 闭环） |
| | LEAK-07 | 49 / 49-07 → 51 / 51-05 | `TestLeak_07_NftLinkLocalDrop_RuleExists`；51-05 注入显式 `169.254.0.0/16 counter drop` 闭 gap | ✓（gap 闭环） |
| | LEAK-08 | 49 / 49-08 → 51 / 51-06 | `TestLeak_08_WorkerCapabilities_Locked`；51-06 删 SYS_ADMIN + drop NET_RAW，NET_ADMIN 按 sing-box tun 依赖保留（fixture `proc_status_clean.txt` 期望需后续放宽，列 TD-1） | ✓（部分闭环；fixture 校准列 tech debt） |
| **KILL** | KILL-01 | 50 / 50-01 | `TestKillSwitch_01_SigkillTiming` + `ClassifyStressResult` 4 分支 + `KillswitchStressContract` 锁定表 | ✓ |
| | KILL-02 | 50 / 50-02 | `TestKillSwitch_02_TunDevDown` + `SetTunDevDown`/`SetTunDevUp` 对称 cleanup | ✓ |
| | KILL-03 | 50 / 50-03 | `TestKillSwitch_03_NetemDelay` + Pumba `gaiaadm/pumba:0.10.0` 固定 tag + `BuildPumbaNetemArgs` + `ParsePumbaOutput` | ✓ |
| | KILL-04 | 50 / 50-04 | `TestKillSwitch_04_NetworkDisconnect` + `PickGatewayBridgeNetwork`（`cloudproxy-net-*` 前缀匹配）+ ReconnectGatewayToBridge 对称 cleanup | ✓ |
| **QUAL** | QUAL-01 | 51 / 51-01 | `internal/network/verify.go` `verifyEgressIPMulti` + `voteEgressIP` + 8 单测 | ✓ |
| | QUAL-02 | 51 / 51-02 | `internal/network/verify.go` `verifyLeakBlockedMulti` + `defaultLeakTargets` 锁定契约 + 5 单测 | ✓ |
| | QUAL-03 | 51 / 51-03 | `internal/network/verify.go` `parseAllNameservers` + ActualDNS 逗号分隔 + 6 单测 | ✓ |
| | QUAL-04 | 51 / 51-04 | `internal/network/namespace.go` `WithProbeWindow`/`WithMaxRetries` functional option + 6 单测 | ✓ |
| | QUAL-05 | 51 / 51-05 | `internal/network/firewall_helpers.go` 全规则 `expr.Counter` + `addIPDaddrCIDRDropRule` + 链末注入 link-local drop | ✓ |
| | QUAL-06 | 51 / 51-06 | `internal/runtime/tasks/worker.go::buildCreateArgs` 删 `--cap-add SYS_ADMIN`、加 `--cap-drop NET_RAW`、保留 `--cap-add NET_ADMIN`（折中） | ✓（折中） |
| | QUAL-07 | 51 / 51-07 | `Makefile` `test-go`/`ci-gate` + `.github/workflows/ci.yml` `go-test` job 默认 `-race -shuffle=on -count=1` | ✓ |
| | QUAL-08 | 51 / 51-08 | `cmd/cloud-claude/testmain_test.go` + `internal/network/testmain_test.go` + `internal/controlplane/app/testmain_test.go` goleak.VerifyTestMain；`go.mod` 新增 `go.uber.org/goleak v1.3.0` | ✓ |
| **OBS** | OBS-01 | 52 / 52-01 | `tests/e2e/harness/collect-artifacts.sh` 172 行 + 7/7 单测（含 `NoAbsoluteUserPathsInScript` 静态扫描） | ✓ |
| | OBS-02 | 52 / 52-02 | 5 子目录 README.md + `metadata.txt` 7 字段（timestamp/scenario/hostname/kernel/git_sha/runner/script_version=v1）+ `CopiesReadmes` 单测 | ✓ |
| | OBS-03 | 52 / 52-03 | `.github/workflows/e2e.yml` `if: failure()` + `actions/upload-artifact@v4` retention 30 天 + PR 评论升级（5 子目录详尽内容 + 两类目录说明）+ `CollectInvokesScript` 单测 | ✓ |

**合计：38/38（100%）满足。**

**额外里程碑内闭环（不在 38 条 v1 REQ 计数）：**

- **D-47-3 双绑互斥后端 pre-check**（Phase 47 列出的 5 项 ROADMAP 偏差之一）→ 51-09 一次性收口。
- **Phase 49 GAP-1（worker capability 残留 NET_RAW / SYS_ADMIN）**→ 51-06 闭环（NET_ADMIN 折中保留）。
- **Phase 49 GAP-2（缺 IPv4 169.254.0.0/16 显式 drop）**→ 51-05 闭环。

---

## 3. 跨 phase 接口约定锁定表

下表列每个 phase 暴露给后续 phase 的 GoldenPath 方法 / 纯函数 / 锁定常量，标记签名稳定度。

| 来源 Phase | 类型 | 符号 | 文件 | 锁定度 |
|------------|------|------|------|--------|
| 45 | Scenario builder | `harness.New(t).WithControlPlane().WithSingBoxGateway(...).WithHost(...).WithUser(...)` | `tests/e2e/harness/scenario.go` | 已稳 |
| 45 | waitFor helpers | `harness.WaitFor` / `WaitForLog` / `WaitForPort` / `WaitForHTTP` / `WaitForExec` | `tests/e2e/harness/waitfor.go` | 已稳 |
| 45 | ArtifactDumper | `ArtifactDumper.Collect(ctx, name) (string, error)` / `OnWaitForTimeout` / `NewArtifactDumper(scenario, baseDir)` | `tests/e2e/harness/artifacts.go` | 已稳（Phase 52 Collect 内部加 runCollectScript 而签名零漂移） |
| 46 | GoldenPath 入口 | `StartGoldenPath(t) *GoldenPath` / `RunBootstrapScript(...)` | `tests/e2e/helpers.go` | 已稳 |
| 46 | 纯函数 / 锁定表 | `Vote` / `EgressIPSources` / `ClassifyDNSResult` / `DefaultDenyMatrix` / `BuildDenyProbeCmd` / `SummarizeDenyResults` / `BootstrapExitCodeContract` / `CLIErrorCases` | `tests/e2e/helpers.go` | 已稳（被 Phase 48/49/50 高频复用） |
| 47 | GoldenPath 方法 | `SimulateExpiry` / `AdminLogin` / `PostBindEgressIP` / `QueryBindingExists` / `KillHostAgent` / `WaitHostHealthStatus` | `tests/e2e/helpers_linux.go` | 已稳 |
| 47 | 锁定常量 / 枚举 | `ExpiryEventType="host.stop.expired"` / `UserExpiredEventType="user.expired"` / `EgressIPDoubleBindContract` / `HostHealthStatus` / `HostHealthRecoveryContract{UnhealthyWithin:30s, HealthyWithin:60s}` | `tests/e2e/helpers.go` | 已稳 |
| 47 | Scenario 扩展 | `(*Scenario).WithControlPlaneEnv(envs map[string]string)` + `controlPlaneSpec.ExtraEnv` | `tests/e2e/harness/scenario.go` | 已稳 |
| 47 | 生产环境变量 | `cmd/control-plane/main.go` 解析 `EXPIRY_SCAN_INTERVAL`（非法 warn 落回 60s 默认） | `cmd/control-plane/main.go` | 已稳（本 phase 唯一生产改动） |
| 48 | GoldenPath 方法 | `KillGateway` / `ProbeOutboundFromUser` / `TcpdumpOnHostEth0` / `InspectContainerIPv4` / `TamperResolvConf` / `ProbeDNSFromUser` | `tests/e2e/helpers_linux.go` | 已稳（Phase 49/50 高频复用） |
| 48 | 锁定常量 / 枚举 | `KillswitchVerdict` / `KillswitchTimingContract{ProbeMaxLatency:3s, TcpdumpWindow:5s}` / `ResolvConfTamperResult` / `ResolvConfTamperContract{Nameserver:"8.8.8.8"}` / `ErrTcpdumpCountNotFound` | `tests/e2e/helpers.go` | 已稳 |
| 48 | 环境变量 | `E2E_TCPDUMP_IMAGE`（默认 `nicolaka/netshoot:v0.13`） / `E2E_ALLOW_HOST_TCPDUMP` | — | 已稳 |
| 49 | GoldenPath 方法 | `DigPlainDNS` / `DigDoT` / `PingICMP` / `CurlIPv6` / `ReadProcFile` / `CurlIMDS` / `TryRawSocket` / `ListNftRulesOnHost` / `GetProcCapabilities` + setup `EnsureWorkerLeakTools` | `tests/e2e/helpers_linux.go` | 已稳 |
| 49 | 纯函数 / 类型 | `LeakProbeResult` / `LeakVerdict` / `ClassifyLeakProbe` / `NftRule` / `ParseNftRules` / `ParseNftCounters` / `HasLinkLocalDropRule` / `Capability` 常量 × 14 / `KnownCapabilityBits` / `ProcCapabilities` / `ParseProcCapabilities` / `LeakDangerousCaps` | `tests/e2e/helpers.go` | 已稳 |
| 50 | GoldenPath 方法 | `SetTunDevDown` / `SetTunDevUp` / `DisconnectGatewayFromBridge` / `ReconnectGatewayToBridge` / `InjectPumbaNetem` / `ProbeSSHBanner` | `tests/e2e/helpers_linux.go` | 已稳 |
| 50 | 纯函数 / 锁定表 | `StressVerdict` / `StressEvidence` / `KillswitchStressContract` / `ClassifyStressResult` / `PumbaNetemParams` / `BuildPumbaNetemArgs` / `PumbaOutcome` / `ParsePumbaOutput` / `PickGatewayBridgeNetwork` | `tests/e2e/helpers.go` | 已稳（KILL-04 兜底 `dockerExecHandle` 临时实现，列 TD-2） |
| 51 | 生产代码改动 | `internal/network/verify.go`（QUAL-01..03 +200 行）/ `namespace.go`（QUAL-04 +50 行）/ `firewall_helpers.go`（QUAL-05 +90 行）/ `worker_firewall_linux.go`（QUAL-05 +20 行）/ `runtime/tasks/worker.go`（QUAL-06 +10/-1 行）/ `controlplane/http/admin_bindings.go`（51-09 +30 行）/ `store/repository/queries.go`（51-09 +12 行）/ `Makefile`（QUAL-07）/ `.github/workflows/ci.yml`（QUAL-07）/ 3 个新 `testmain_test.go`（QUAL-08） | — | 已稳 |
| 51 | 新依赖 | `go.uber.org/goleak v1.3.0`（go.mod / go.sum） | — | 已稳（ignore list 仅 `broadcast.(*Hub).cleanupLoop`） |
| 51 | API 错误码 | `ErrCodeEgressIPAlreadyBound` 常量 + HTTP 409 + 中英双语 message + host_id / egress_ip_id 字段回显 | `internal/controlplane/http/admin_bindings.go` | 已稳 |
| 52 | 脚本侧入口 | `bash tests/e2e/harness/collect-artifacts.sh <output-dir> [scenario-id]`；`SCRIPT_VERSION="v1"` 锁定，扩展 metadata.txt 字段需升 v2 | `tests/e2e/harness/collect-artifacts.sh` | 已稳 |
| 52 | 输出树两类目录 | `out/e2e-artifacts/<sanitized-test-name>/<timestamp>/`（用例级，ArtifactDumper.Collect）+ `out/e2e-artifacts/ci-<job>-<attempt>/`（runner 全局快照，e2e.yml 一次性） | — | 已稳 |
| 52 | CI workflow 锁定 | e2e.yml `if: failure()` 三连（脚本采集 → upload-artifact@v4 → PR 评论 5 子目录详尽内容）；artifact name `e2e-artifacts-${{ github.run_id }}-${{ github.run_attempt }}`；retention 30 天 / `if-no-files-found: warn` | `.github/workflows/e2e.yml` | 已稳 |

**跨 phase 契约结论：**所有暴露符号全部 WIRED，无悬空契约。Phase 45 Plan 02 Scenario.Start Step 2..7 sentinel 是唯一「骨架就位但真实接入待后续 milestone」的项，已显式记录在 TD-3，**不构成跨 phase 悬空**（下游 Phase 46+ 用例侧通过 `StartGoldenPath` 在 sentinel 缺失时 `t.Skip`）。

---

## 4. Tech Debt 表

| ID | 来源 | 描述 | 优先级 | Ship 阻塞？ |
|----|------|------|--------|-------------|
| TD-1 | Phase 49 LEAK-08 + Phase 51 QUAL-06 | **fixture `proc_status_clean.txt` NET_ADMIN 期望与 Phase 51 折中保留 NET_ADMIN 的一致性校准**：51-06 落地后 worker CapBnd 实际仍含 NET_ADMIN（sing-box 依赖），但 LEAK-08 fixture 严格期望「CapBnd 不含 NET_ADMIN」，Linux runner 跑 `TestLeak_08_WorkerCapabilities_Locked` 时会从「BACKEND GAP」分支转为「fixture 期望与实际不符」语义 fail；需调整 fixture 期望或在用例侧明确「NET_ADMIN 允许保留」白名单 | P1 | 否（CONTEXT §Area 4 明确允许折中） |
| TD-2 | Phase 50 KILL-04 SUMMARY | **`dockerExecHandle` 临时 ContainerHandle 实现**：KILL-04 用 ContainerHandle 接口最小实现来包一次 `docker exec`，等 Phase 52 OBS-* 引入正式 testcontainers Container 后可移除 | P2 | 否 |
| TD-3 | Phase 45 Plan 02 + Phase 46 SUMMARY | **Scenario.Start Step 2..7 真实接入（仍是 sentinel）**：当前 Step 1（postgres testcontainer）真实，Step 2..7（控制面子进程 / admin login / fixture 三件套 / PrepareGateway / PrepareHost）留 TODO；下游所有 Linux runner 用例通过 `StartGoldenPath` 在 sentinel 缺失时 `t.Skip` 兜底；Linux runner 真实跑通的前置 | P1 | 否（darwin 维度全绿，Linux 真机 deferred-to-CI） |
| TD-4 | Phase 47 CONTEXT D-47-4 | **host-agent per-host health API 缺失**：当前仅全局 `GET /healthz`（无 admin guard），通过 `checks.agent` 字段表达 host-agent 进程级健康；多宿主机场景下需要 `GET /v1/admin/hosts/{id}/health` per-host endpoint，属 Phase 50+ 多宿主机阶段 deferred | P2 | 否（v1 单宿主机假设下不影响） |
| TD-5 | Phase 46 MVS-05 SUMMARY | **fixture SQL 中 bcrypt hash 动态生成**：当前 `tests/e2e/fixtures/error-codes.sql` 含硬编码 bcrypt hash，未来用 helper 函数动态生成填充避免过期 hash | P2 | 否 |
| TD-6 | Phase 45 Plan 05 SUMMARY + Phase 46 SUMMARY | **hosted ubuntu-24.04 上 `/dev/net/tun` 偶发不可用的 fixture preflight 兜底**：当前 sing-box 真实用例缺 preflight 检测，CI 上若 runner 内核未启用 tun 模块会直接 fail；需 e2e.yml 加 preflight `[ -e /dev/net/tun ] && ...` 或 fallback 路径 | P2 | 否 |
| TD-7 | Phase 52 VERIFICATION 遗留清单 | **`DATABASE_URL` 透传给 CI failure step 实现 testcontainer PG 真实 schema dump**：当前 collect-artifacts.sh 在 CI 端无 `DATABASE_URL` 时 postgres/ 子目录写 `_skipped.txt`；需 e2e.yml 把 testcontainer PG 的连接串透传到 failure step 之 env | P2 | 否 |
| TD-8 | Phase 52 VERIFICATION 遗留清单 + Phase 45 SUMMARY | **fork PR 上 PR 评论 403 的 fallback**（如 `pull_request_target` + 严格输入校验）：当前 `actions/github-script@v7` 在 fork PR 上写评论会因 GITHUB_TOKEN 权限路径 403；fork PR 成为常态时需补救 | P3 | 否 |

**Tech Debt 合计：8 项**，0 项阻塞 ship。TD-1 / TD-3 为 P1，建议作为 v3.7+ 起手第一周收尾工作；TD-2 / TD-4 / TD-5 / TD-6 / TD-7 为 P2，可与 v3.7 主路径并行；TD-8 为 P3。

---

## 5. Human Verification Pending 表

汇总 8 个 phase VERIFICATION 中的 `human_verification` / `deferred-to-CI` 项，按里程碑 sign-off 优先级排序。

| 优先级 | 来源 Phase | 验证项 | 前置条件 | 期望结果 |
|--------|------------|--------|----------|----------|
| **P0 — 黄金路径** | 46 / 47 | MVS-01 events.host.ready 双重确认 + MVS-02 公网 IP 等值断言 + MVS-06 `TestExpiry_AutoStop_GoldenPath` + MVS-08 `TestHostAgent_KillRecover_GoldenPath` | TD-3（Scenario Step 2..7 真实接入）+ `EXPIRY_SCAN_INTERVAL=1s` env 透传 + host-agent 非 embedded 模式 | 30s 内容器自动停止 + events 出现 `host.stop.expired` 行；30s 内 `checks.agent` 转 unreachable，60s 内回到 ok |
| **P0 — 双绑闭环** | 47 / 51 | MVS-07 `TestEgressIPBinding_DoubleBindExcluded`（51-09 落地后预期走 PASS 分支） | admin login fixture | status=409 + `error_code="egress_ip_already_bound"` + 中英双语 message + A 原绑定不破坏 + B 不被意外写入 |
| **P0 — kill-switch 核心** | 48 / 50 | MVS-09 SingboxCrash + MVS-10 ResolvConfTamper + KILL-01 SigkillTiming + KILL-02 TunDevDown + KILL-04 NetworkDisconnect | TD-3 + `docker pull nicolaka/netshoot:v0.13` | 3s 内 worker 断网 + tcpdump 窗口 0 包 |
| **P1 — 防泄漏 8 向量** | 49 / 51 | LEAK-01..05 + LEAK-06（51-06 闭环后预期 PASS）+ LEAK-07（51-05 闭环后预期 PASS） | TD-3 + `EnsureWorkerLeakTools` | 8 用例全 PASS；nft 规则集含显式 link-local drop |
| **P1 — capability 审计** | 49 / 51 | LEAK-08（51-06 部分闭环；fixture 校准属 TD-1） | TD-1 fixture 调整 | CapEff/CapBnd 不含 NET_RAW + SYS_ADMIN；NET_ADMIN 在白名单内 |
| **P1 — netem 故障注入** | 50 | KILL-03 NetemDelay（Pumba sidecar） | `docker pull gaiaadm/pumba:0.10.0 gaiadocker/iproute2` | SSH banner 仍存活 + 出口 IP 投票 Inconclusive |
| **P2 — artifact 收集真机签字** | 52 | VERIFY-1（hosted ubuntu-24.04 故意失败 → artifact zip 内 5 子目录有真实输出） | CI workflow 已就绪 | network/nft-ruleset.txt 非空 + docker/ps.txt 含容器列表 + system/dmesg-tail.txt 含真实内核日志 |
| **P2 — PR 评论真机签字** | 52 | VERIFY-2（同仓 PR 失败时 PR 评论自动出现 5 子目录详尽内容） | 真实 PR 跑通失败用例 | github-script@v7 评论 body 出现两类目录说明 |
| **P3 — preflight 兜底** | 45 | hosted ubuntu-24.04 `/dev/net/tun` 偶发不可用的 fixture preflight（TD-6） | TD-6 落地 | preflight 检测通过即跑全套件 |

**Human Verification Pending 合计：9 项（去重后）**，全部 deferred-to-CI 非阻塞 ship。TD-3「Scenario Step 2..7 真实接入」是 P0/P1 全部签字项的共同前置，建议作为下一里程碑起手第一周的收尾工作。

---

## 6. 风险评估

### 6.1 本里程碑改了哪些生产代码

**仅 Phase 51**（按 CONTEXT §Area 4 锁定的「e2e 用例尽可能不动生产代码」原则，只有 Phase 51 主动收紧质量加固，其它 phase 全部走 e2e 用例 + harness 抽象 + 纯函数）：

| 文件 | 改动行数 | 来源 Plan | 影响面 |
|------|---------|-----------|--------|
| `internal/network/verify.go` | +200 行 | 51-01 / 51-02 / 51-03 | 出口 IP / 泄漏检测 / DNS 校验从单源 / 单目标 / 首行升级为多源 / 多目标 / 全行；外部契约（API 签名 + 返回字段）不变，仅「内部更稳健」 |
| `internal/network/namespace.go` | +50 行 | 51-04 | `GetContainerNetNS` 暴露 `WithProbeWindow` / `WithMaxRetries` functional option；旧调用零修改（默认 fallback） |
| `internal/network/firewall_helpers.go` | +90 行 | 51-05 | 所有 build/add 规则插入 `expr.Counter{}`；新增 `buildIPDaddrCIDRDropExprs` + `addIPDaddrCIDRDropRule` helper |
| `internal/network/worker_firewall_linux.go` | +20 行 | 51-05 | `applyWorkerIPv4Rules` 在 OUTPUT 链 lo/ESTABLISHED 之后注入 `169.254.0.0/16 counter drop comment "linklocal-drop"` |
| `internal/runtime/tasks/worker.go` | +10 行 / -1 行 | 51-06 | `buildCreateArgs` 删 `--cap-add SYS_ADMIN`、加 `--cap-drop NET_RAW`、保留 `--cap-add NET_ADMIN` |
| `internal/controlplane/http/admin_bindings.go` | +30 行 | 51-09 | `Bind` pre-check 路径：SELECT lookup → 409 + `error_code="egress_ip_already_bound"` + 中英双语 message + host_id / egress_ip_id 回显 |
| `internal/store/repository/queries.go` | +12 行 | 51-09 | 新增 `GetBindingHostIDByEgressIP` |
| `cmd/control-plane/main.go` | +少量行 | 47-01 | 解析 `EXPIRY_SCAN_INTERVAL` 环境变量（Phase 47 唯一允许的生产改动） |
| `Makefile` | +3 行 | 51-07 | `test-go` / `ci-gate` 默认 `-race -shuffle=on -count=1` |
| `.github/workflows/ci.yml` | +3 行 / -1 行 | 51-07 | `go-test` job 默认 `-race -shuffle=on -count=1` |
| `.github/workflows/e2e.yml` | +30 行 diff | 45-05 / 52-03 | 新建 e2e workflow + Phase 52 完整版 PR 评论 + failure-only 步骤 |
| `go.mod` / `go.sum` | 1 新依赖 | 51-08 | `go.uber.org/goleak v1.3.0`（本里程碑唯一允许的新生产依赖） |

**生产代码新增 ≈ 445 行**（不计测试 / fixture / workflow），改动集中在 `internal/network/` + `internal/controlplane/http/` + `internal/store/repository/` + `internal/runtime/tasks/`，全部走 `go test ./... -race -shuffle=on -count=1` 全绿 + 19 包零回归。

### 6.2 生产代码改动的回归风险

- **darwin 维度全绿**：`go test ./... -race -shuffle=on -count=1` 19 包全 PASS；既有 `internal/network/*_test.go` / `internal/runtime/tasks/*_test.go` / `internal/controlplane/http/*_test.go` 零修改 + 100% 通过；本里程碑新增 32 个生产代码层单测（QUAL-01..08 + 51-09）全绿。
- **Linux 真机未签**：`tests/e2e/` 套件的 8 个 LEAK + 4 个 KILL + 3 个 MVS-* 用例骨架就位，darwin 全 `t.Skip` 等 Linux runner 真实跑；TD-3 完成前 Linux runner 不构成生产回归证据，但每个用例都内建 sentinel 分支（`StartGoldenPath` 检测缺失字段时 `t.Skipf`），不会 false-positive 阻塞 CI。
- **goleak 接入**：QUAL-08 三个 `testmain_test.go` 在 `internal/network` / `cmd/cloud-claude` / `internal/controlplane/app` 跑 `goleak.VerifyTestMain`，ignore list 仅 `broadcast.(*Hub).cleanupLoop`（设计内常驻），有效拦截非已知模式的 goroutine 泄漏。
- **`-race -shuffle=on -count=1` 默认化**：QUAL-07 落地后 CI 与本地 `make test-go` 都强制 race detector + 用例随机执行顺序，对竞争条件 / 隐式顺序依赖具备持续守护。

### 6.3 是否影响 v3.0+ 已上线功能

| 已上线功能 | 风险来源 | 评估 |
|------------|----------|------|
| bootstrap 入口（v1.0 / v2.0） | 51-09 双绑 pre-check 改 admin_bindings.go Bind 路径 | **零影响**：bootstrap 链路通过 `entry-token` 走 `GET /v1/admin/hosts/by-entry-token`，不经 `POST /v1/admin/hosts/{id}/egress-bindings` |
| SSH 接入（v1.0 / v2.0 / v3.0） | 51-06 worker cap-drop NET_RAW | **零影响**：OpenSSH 不需要 CAP_NET_RAW；51-06 只删了原本就未使用的 SYS_ADMIN |
| cloud-claude CLI（v2.0 / v3.0） | QUAL-08 goleak 接入 `cmd/cloud-claude/` | **零影响**：goleak 只在 `*_test.go` 生效，二进制 / 运行时无开销 |
| KasmVNC 用户面（v1.2 partial） | — | **零影响**：本里程碑不动 KasmVNC 任何路径 |
| FUSE / sshfs 映射（v3.0 / v3.1） | — | **零影响**：本里程碑不动 FUSE / sshfs 任何路径 |
| Dev Containers / VS Code Remote（v3.4） | — | **零影响**：本里程碑不动 Dev Containers / VS Code 任何路径 |

### 6.4 是否影响 v3.5 已上线网络栈

| v3.5 网络栈 | 风险来源 | 评估 |
|--------------|----------|------|
| sing-box 两段式静态配置 + 动态 rule-set 文件 watch | QUAL-05 nft 全规则 counter + 链末 link-local drop | **零影响**：counter 只是观测属性不改 drop 行为；link-local drop 在 OUTPUT 链 lo/ESTABLISHED accept 之后、白名单/DNS/gwIP accept 之前注入，不影响 sing-box auto_route 主路径；Phase 49 LEAK-05 IMDS 用例**不会回归**（行为原本由链末 sbfw-drop 兜住，新规则只是更显式） |
| 拆分 DNS（内网 `.lan/.local/.internal` → dns-local；公网白名单 → 代理 DoH） | QUAL-03 verifyDNS 遍历全 nameserver | **零影响**：仅扩展 ActualDNS 上报字段为逗号分隔，校验语义不变 |
| `ContainerProxyProvider` PrepareGateway + PrepareHost | — | **零影响**：本里程碑不改 `internal/network/container_proxy_provider.go` 任何路径 |
| `is_system` 预设双层防御 + ErrSystemBypassPresetImmutable | — | **零影响** |
| 双轨审计（DB audit_log + EventRecorder） | 47-01 `host.stop.expired` 事件名锁定 | **零影响**：事件名与现有 EventRecorder 接口契约一致；e2e 侧只读不写 |
| uat-bypass.sh I1..I10 守护 | — | **零影响**：本里程碑不改 uat-bypass.sh |

**风险结论：darwin 维度 0 回归，Linux 真机维度风险等同于 v3.6 deferred-to-CI 项（9 个签字点）的覆盖范围**；TD-3 完成后跑通即闭环。

---

## 7. Audit 结论

**status: tech_debt（ship_with_followups）**

### 7.1 为何不是 `passed`

- TD-1 `proc_status_clean.txt` fixture 与 Phase 51 折中保留 NET_ADMIN 的一致性校准尚未做（属 Phase 49 范围，本里程碑 Phase 51 仅生产代码层落地）。
- TD-3 Scenario.Start Step 2..7 仍是 sentinel（Linux runner 真机端到端跑通的共同前置）。

两项均不阻塞 ship（CONTEXT §Area 4 明确允许「darwin 编译 + 纯函数单测 PASS = passed；Linux 真机断言 deferred-to-CI 非阻塞」），但严格按 ROADMAP §Phase 49 §Details 1「LEAK-08 worker CapBnd 不含 NET_RAW/NET_ADMIN/SYS_ADMIN」字面承诺，NET_ADMIN 折中保留与 fixture 期望存在显式偏差，达不到 `passed` 标准。

### 7.2 为何不是 `gaps_found`

- 38/38 v1 REQ-ID 全部 SATISFIED（含 Phase 49 三条 backend gap 在 Phase 51 同里程碑内闭环 + Phase 47 D-47-3 双绑 pre-check 在 51-09 收口）。
- 8 phase VERIFICATION.md `status` 全部 `passed`（Phase 49 初次 `gaps_found`，按里程碑维度看「闭环后实际状态」是 `passed`，仅 LEAK-08 fixture 校准列 tech debt）。
- 所有跨 phase 接口约定（GoldenPath 方法 / 纯函数 / 锁定常量 / 环境变量 / Scenario builder / CI workflow）全部 WIRED，无悬空契约。
- 测试门：`go build ./...` × `GOOS=linux go build ./...` × `GOOS=linux go build -tags='e2e linux' ./tests/e2e/...` × `go test ./... -race -shuffle=on -count=1` × `go vet ./...` × `bash scripts/lint-no-bare-sleep.sh` 六道闸全绿；`go test -tags=e2e ./tests/e2e/harness/` 23 个单测全 PASS。
- 隐私守护：8 个 phase VERIFICATION + 7 个 README + 主脚本 `collect-artifacts.sh` grep `/Users/<user>/` / `/home/<user>/` / 个人邮箱 → **0 命中**。
- 零阻塞 human-UAT（不同于 v3.0/v3.1/v3.4 的「上线后用户真机签字」属性，本里程碑全部 deferred-to-CI 项都是「CI runner 跑通即闭环」，不需要用户参与）。

### 7.3 Ship 建议

**可以 ship**（status: tech_debt），附带显式 follow-up 跟踪：

1. **TD-1 + TD-3（P1，最优先）：** v3.7+ 起手第一周内：（a）Phase 45 Plan 02 Scenario.Start Step 2..7 真实落地（控制面子进程 / admin login / fixture 三件套 / PrepareGateway / PrepareHost）；（b）Phase 49 LEAK-08 fixture `proc_status_clean.txt` 调整 NET_ADMIN 期望或加白名单语义。两者完成后 9 个 human verification 项可在 hosted ubuntu-24.04 上一次性签字。
2. **TD-2 / TD-4..TD-7（P2，可批量）：** 与 v3.7 主路径并行收尾；TD-4 host-agent per-host health API 待 v4 多宿主机阶段视情况引入。
3. **TD-8（P3）：** 当 fork PR 真正成为常态时再补 fallback；当前同仓 PR 完全可用。

---

## 8. 给下一里程碑（v3.7+）的建议

| # | 建议 | 来源 | 类型 |
|---|------|------|------|
| 1 | **Phase 45 Plan 02 Scenario.Start Step 2..7 真实接入** —— Linux runner 跑通的共同前置，建议作为 v3.7+ 起手第一周的 P0 收尾工作 | TD-3 | 收尾 |
| 2 | **Phase 49 LEAK-08 fixture 校准** —— 调整 `proc_status_clean.txt` NET_ADMIN 期望或加白名单语义，与 Phase 51 QUAL-06 折中决策一致 | TD-1 | 收尾 |
| 3 | **完整 Linux runner sign-off 走过一遍** —— hosted ubuntu-24.04 跑通 9 项 human verification（MVS-06/07/08 + MVS-09/10 + LEAK-01..08 + KILL-01..04 + VERIFY-1/2），覆盖 v3.6 全部 deferred-to-CI 项；建议作为 v3.7 第一个 PR 的「冒烟」附带跑通 | P0/P1 human verification 表 | 收尾 |
| 4 | **`DATABASE_URL` 透传给 CI failure step** —— 让 collect-artifacts.sh 在 CI 端真实 `pg_dump --schema-only` | TD-7 | 增强 |
| 5 | **artifact 体积自动裁剪 / 压缩** —— 当 artifact zip 超 100MB 时自动滚动裁剪或 gzip | ROADMAP §Phase 52 §Details 2 | 增强 |
| 6 | **Tetragon TracingPolicy 内核 oracle 集成（v2 范围 / E2E-FULL-03）** —— 本里程碑明确列入 v2 deferred；下一里程碑视 runner 资源情况引入 | REQUIREMENTS.md §v2 | v2 范围 |
| 7 | **并发 N=50/100 容器无残留（E2E-FULL-01）+ conntrack flush（E2E-FULL-02）+ host 重启清理（E2E-FULL-04）** —— v2 范围扩展矩阵 | REQUIREMENTS.md §v2 | v2 范围 |
| 8 | **Hurl 控制面 API 断言矩阵（E2E-FULL-05）** —— 后台 CRUD / IP 绑定 / 生命周期 | REQUIREMENTS.md §v2 | v2 范围 |
| 9 | **冷启动端到端时间 ≤ 15s（E2E-PERF-01）+ 完整矩阵 ≤ 60min nightly（E2E-PERF-02）** —— 性能基线 | REQUIREMENTS.md §v2 | v2 范围 |
| 10 | **host-agent per-host health API（多宿主机场景）** —— 当多宿主机阶段开始时引入 `GET /v1/admin/hosts/{id}/health` | TD-4 | v4+ |
| 11 | **fork PR 上 PR 评论 403 的 fallback** —— 当 fork PR 成为常态时补 `pull_request_target` + 严格输入校验路径 | TD-8 | 视情况 |
| 12 | **fixture SQL bcrypt hash 动态生成** —— 避免硬编码过期 hash；CI 接通后由 SeedBootstrapErrorFixtures helper 函数填充 | TD-5 | 增强 |

---

## 9. 提交记录（关键 commit 链）

**Phase 45（测试基础设施 + CI 骨架）：**
```
2fc9990 docs(45): smart discuss context
bca7e78 docs(45): 拆出 Phase 45 五个 PLAN.md
d4c0262 feat(45-01): e2e 测试基础设施骨架（testcontainers + testify + build tag）
29f526f feat(45-03): waitFor helper + 4 个语义化变体 + DumpHook 占位
97bea7f feat(45-02): Scenario builder API 骨架 + Step 1 真实 + Step 2..7 TODO
fdf8b33 feat(45): Wave 3+4 收尾 — artifact dump hook + CI 双层守护 + Phase 45 verification
```

**Phase 46（MVS 黄金路径）：**
```
c9986d8 docs(46): smart discuss context
a77ffb3 docs(46): 拆出 Phase 46 五个 PLAN.md（MVS-01..05）
9264d85 feat(46-01): bootstrap 黄金路径骨架 + GoldenPath 抽象 + 24 个纯函数单测
6d1e6cc feat(46-02): 出口 IP 三源轮询 + Vote 多数派裁决
d127957 feat(46-03): DNS 走 tun OR 防火墙拒绝
95a99a2 feat(46-04): 默认拒绝矩阵
4fae5e9 feat(46-05): CLI 错误码契约 e2e 骨架 + fixture SQL
9ebd68c docs(46): Phase 46 VERIFICATION.md
4ce0cd5 docs(46): mark Phase 46 plans complete
```

**Phase 47（MVS 治理）：**
```
1ad1fa6 docs(47): smart discuss context
6eefe9a docs(47): 拆出 Phase 47 三个 PLAN.md
2782167 feat(47-01): 到期容器自动停止 + host.stop.expired 审计事件 (MVS-06)
077515d feat(47-02): 出口 IP 双绑互斥 e2e 用例 (MVS-07)
8e2cc8f feat(47-03): host-agent 心跳与恢复 e2e 用例 (MVS-08)
0033096 docs(47): Phase 47 VERIFICATION.md
2ee6b0d docs(47): mark Phase 47 plans complete
```

**Phase 48（kill-switch 核心）：**
```
5230ae4 docs(48): smart discuss context
fcfb6dd docs(48): 拆出 Phase 48 两个 PLAN.md
2fca349 feat(48-01): sing-box 崩溃后用户容器立即断网 e2e 用例 (MVS-09)
433bf7a feat(48-02): 容器内 resolv.conf 篡改免疫 e2e 用例 (MVS-10)
d7b6548 docs(48): Phase 48 VERIFICATION.md
01e1def docs(48): mark Phase 48 plans complete
```

**Phase 49（防泄漏对抗）：**
```
6e9686f docs(49): smart discuss context
79f1811 docs(49): 拆出 Phase 49 八个 PLAN.md (LEAK-01..08)
15f2397 feat(49-shared): LEAK helpers 与纯函数 + 单测
577d38e..6ec141a feat(49-01..08): LEAK-01..08 e2e 用例
5ad888b docs(49): Phase 49 VERIFICATION.md
1f2f919 docs(49): ROADMAP 标记 8 个 plan 完成 + 3 条 gap 流转 Phase 51
3112463 docs(49): bump STATE to 23/38
```

**Phase 50（kill-switch 压力）：**
```
2850c6c docs(50): smart discuss context
9441873 docs(50): 拆出 Phase 50 四个 PLAN.md (KILL-01..04)
037544d feat(50-shared): KILL-* 压力测试共享类型与 Pumba/网络 helpers
536780d..b1c0db6 feat(50-01..04): KILL-01..04 e2e 用例
90a598b docs(50): Phase 50 VERIFICATION.md
dc4854a docs(50): mark Phase 50 plans complete
```

**Phase 51（代码层质量加固）：**
```
e42528a docs(51): smart discuss context
dda44c2 docs(51): 拆出 Phase 51 九个 PLAN.md (QUAL-01..08 + 双绑 API)
e146b3f feat(51-04): GetContainerNetNS 探测窗口参数化
92b5e56 feat(51-07): go test 默认 -race -shuffle=on
1bb7831 feat(51-08): goleak.VerifyTestMain 接入
2d51db3 feat(51-01): verifyEgressIP 多源轮询 + 多数派投票
c5d809b feat(51-02): verifyLeakBlocked 多目标矩阵化
dccebd3 feat(51-03): verifyDNS 遍历全部 nameserver
0f5228e feat(51-05): nft 全规则加 counter + 169.254/16 显式 drop
c84c86d feat(51-06): worker cap-drop NET_RAW + 删 SYS_ADMIN
c290810 feat(51-09): 双绑互斥 API pre-check + 稳定 error code
e1de5c9 docs(51): Phase 51 VERIFICATION.md
58edc22 docs(51): mark Phase 51 9 plans complete + bump STATE to 36/38
```

**Phase 52（可观测性）：**
```
bdbdfce docs(52): smart discuss context
c4ab258 docs(52): 拆出 Phase 52 三个 PLAN.md (OBS-01..03)
1de1a4c feat(52-01): collect-artifacts.sh 一键采集脚本 + 6 unit test
2216d55 feat(52-02): 5 子目录 README 模板 + Plan 02 单测扩展
8f8fc30 feat(52-03): DumpHook 切到脚本 + e2e.yml upload-artifact 完整化
b88ef43 docs(52): Phase 52 VERIFICATION.md
16c1422 docs(52): mark Phase 52 + entire v3.6 milestone complete (39/39)
```

---

## AUDIT COMPLETE: tech_debt

_Audited: 2026-05-14_
_Auditor: Claude (gsd-audit-milestone)_
_Tip commit: `16c1422 docs(52): mark Phase 52 + entire v3.6 milestone complete (39/39)`_

**结论一句话：v3.6 ship-ready，38/38 v1 REQ satisfied，8/8 phase VERIFICATION passed（Phase 49 初次 gaps_found 已在 Phase 51 同里程碑内闭环），8 项 tech debt 全部非阻塞，9 项 human verification 全部 deferred-to-CI；进 complete-milestone 流程，TD-1/TD-3 列入 v3.7+ 起手第一周收尾。**
