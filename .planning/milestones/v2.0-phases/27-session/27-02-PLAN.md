---
phase: 27-session
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - internal/cloudclaude/ssh.go
  - cmd/cloud-claude/main.go
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03]

must_haves:
  truths:
    - "ConnectAndRunClaude 内部按 connect → mountWorkspace → runClaude 三阶段执行"
    - "两个 session（sshfs + claude PTY）共享同一个 ssh.Client，不建立第二条 TCP 连接"
    - "claude 在容器内以 /workspace 为工作目录运行"
    - "defer 链保证清理顺序：fusermount → close sshfs session → close SSH connection"
    - "main.go 传递当前工作目录给 ConnectAndRunClaude"
  artifacts:
    - path: "internal/cloudclaude/ssh.go"
      provides: "三阶段 ConnectAndRunClaude（sshConnect + mountWorkspace + runClaude）"
      contains: "func sshConnect"
    - path: "cmd/cloud-claude/main.go"
      provides: "CWD 获取并传递到 ConnectAndRunClaude"
      contains: "os.Getwd()"
  key_links:
    - from: "internal/cloudclaude/ssh.go"
      to: "internal/cloudclaude/mount.go"
      via: "ConnectAndRunClaude 调用 mountWorkspace(conn, cwd)"
      pattern: "mountWorkspace"
    - from: "internal/cloudclaude/ssh.go"
      to: "shellescape.QuoteCommand"
      via: "构建 cd /workspace && claude <args> 远程命令"
      pattern: "cd /workspace"
    - from: "cmd/cloud-claude/main.go"
      to: "internal/cloudclaude/ssh.go"
      via: "ConnectAndRunClaude(sshCfg, args, cwd)"
      pattern: "ConnectAndRunClaude"
---

<objective>
重构 ConnectAndRunClaude 为三阶段架构（connect → mountWorkspace → runClaude），将目录映射集成到 CLI 主流程中。

Purpose: 将 Plan 01 创建的 mount 基础设施接入 SSH 连接生命周期，实现 Phase 27 端到端目标——用户运行 cloud-claude 时自动映射当前目录到容器 /workspace，claude 以 /workspace 为工作目录运行，退出时自动清理。
Output: 重构后的 ssh.go + 更新的 main.go。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-session/27-CONTEXT.md
@.planning/phases/27-session/27-RESEARCH.md
@.planning/phases/27-session/27-PATTERNS.md

@internal/cloudclaude/ssh.go
@internal/cloudclaude/mount.go
@cmd/cloud-claude/main.go

<interfaces>
<!-- Plan 01 产出的 mount.go 接口（executor 直接使用） -->

From internal/cloudclaude/mount.go (Plan 01 创建):
```go
func mountWorkspace(conn *ssh.Client, localDir string) (cleanup func(), err error)
func fusermountCleanup(conn *ssh.Client)
```

From internal/cloudclaude/ssh.go (当前状态):
```go
type SSHConfig struct {
    Host     string
    Port     int
    User     string
    Password string
}

func ConnectAndRunClaude(cfg SSHConfig, claudeArgs []string) (int, error)
```

From cmd/cloud-claude/main.go (当前调用方式):
```go
exitCode, err := cloudclaude.ConnectAndRunClaude(sshCfg, args)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构 ssh.go 为三阶段架构</name>
  <files>internal/cloudclaude/ssh.go</files>
  <read_first>
    - internal/cloudclaude/ssh.go（当前完整实现，115 行，需理解全部逻辑后再拆分）
    - internal/cloudclaude/mount.go（Plan 01 产出，确认 mountWorkspace 签名）
    - .planning/phases/27-session/27-CONTEXT.md §D-03, D-04, D-05, D-09（三阶段、启动顺序、cd /workspace、defer 链）
    - .planning/phases/27-session/27-RESEARCH.md §Pattern 2（三阶段启动时序代码示例）
  </read_first>
  <action>
重构 `internal/cloudclaude/ssh.go`，将现有 `ConnectAndRunClaude` 拆分为三个内部阶段函数。

**Step 1: 修改 ConnectAndRunClaude 签名**

旧签名：`func ConnectAndRunClaude(cfg SSHConfig, claudeArgs []string) (int, error)`
新签名：`func ConnectAndRunClaude(cfg SSHConfig, claudeArgs []string, cwd string) (int, error)`

新增 `cwd string` 参数，表示用户当前工作目录，传递给 mountWorkspace。

**Step 2: 提取 sshConnect 函数**

将 ConnectAndRunClaude 中 lines 28-48 的 SSH 连接建立逻辑提取为独立函数：

```go
func sshConnect(cfg SSHConfig) (*ssh.Client, error) {
    clientCfg := &ssh.ClientConfig{
        User: cfg.User,
        Auth: []ssh.AuthMethod{
            ssh.Password(cfg.Password),
        },
        HostKeyCallback: ssh.InsecureIgnoreHostKey(),
        Timeout:         10 * time.Second,
    }

    addr := net.JoinHostPort(cfg.Host, fmt.Sprintf("%d", cfg.Port))
    tcpConn, err := net.DialTimeout("tcp", addr, 10*time.Second)
    if err != nil {
        return nil, fmt.Errorf("SSH 连接失败（无法连接 %s）: %w", addr, err)
    }

    sshConn, chans, reqs, err := ssh.NewClientConn(tcpConn, addr, clientCfg)
    if err != nil {
        tcpConn.Close()
        return nil, fmt.Errorf("SSH 握手失败: %w", err)
    }
    return ssh.NewClient(sshConn, chans, reqs), nil
}
```

**Step 3: 提取 runClaude 函数**

将 ConnectAndRunClaude 中 lines 51-114 的 session 创建、PTY、raw mode、SIGWINCH、命令执行、退出码处理逻辑提取为独立函数：

```go
func runClaude(conn *ssh.Client, claudeArgs []string) (int, error)
```

关键改动：远程命令从原来的 `shellescape.QuoteCommand(append([]string{"claude"}, claudeArgs...))` 改为 `cd /workspace && <原命令>`。具体实现（per D-05）：

```go
claudeCmd := shellescape.QuoteCommand(append([]string{"claude"}, claudeArgs...))
remoteCmd := "cd /workspace && " + claudeCmd
```

其余逻辑（PTY 申请、raw mode、SIGWINCH 监听、stdin/stdout/stderr 绑定、Wait + 退出码处理）从原 ConnectAndRunClaude 原样保留，不做修改。

**Step 4: 重写 ConnectAndRunClaude 主体**

新的 ConnectAndRunClaude 内部编排三阶段（per D-03, D-04, D-09）：

```go
func ConnectAndRunClaude(cfg SSHConfig, claudeArgs []string, cwd string) (int, error) {
    // Phase 1: 建立 SSH 连接
    conn, err := sshConnect(cfg)
    if err != nil {
        return 0, err
    }
    defer conn.Close()

    // Phase 2: 挂载工作目录（per D-04: 先挂载后运行 claude）
    cleanupMount, err := mountWorkspace(conn, cwd)
    if err != nil {
        return 0, fmt.Errorf("目录映射失败: %w", err)
    }
    defer cleanupMount()

    // Phase 3: 运行 claude
    return runClaude(conn, claudeArgs)
}
```

**defer 顺序（per D-09）：** `conn.Close()` 先声明（最后执行）→ `cleanupMount()` 后声明（先执行）。cleanupMount 内部执行顺序：关闭 sshfs session → 等待 SFTP goroutine → fusermountCleanup。fusermountCleanup 需要 conn 仍然打开，这在 LIFO defer 下是保证的。

**不改动的部分：**
- SSHConfig 结构体定义不变
- import 列表可能需要删除不再直接使用的包（重构后 ConnectAndRunClaude 本身不再直接使用 os、term 等，但 runClaude 仍需要，都在同一文件中所以 import 不变）
  </action>
  <verify>
    <automated>cd . && go build ./internal/cloudclaude/...</automated>
  </verify>
  <acceptance_criteria>
    - ssh.go 包含 `func sshConnect(cfg SSHConfig) (*ssh.Client, error)`
    - ssh.go 包含 `func runClaude(conn *ssh.Client, claudeArgs []string) (int, error)`
    - ssh.go 包含 `func ConnectAndRunClaude(cfg SSHConfig, claudeArgs []string, cwd string) (int, error)`
    - ssh.go 包含 `mountWorkspace(conn, cwd)`
    - ssh.go 包含 `defer cleanupMount()`
    - ssh.go 包含 `cd /workspace && `（远程命令前缀）
    - ssh.go 包含 `defer conn.Close()`
    - ssh.go 中 `sshConnect` 返回 `*ssh.Client`（不再在 ConnectAndRunClaude 中内联）
    - `go build ./internal/cloudclaude/...` 退出码为 0
  </acceptance_criteria>
  <done>ConnectAndRunClaude 拆分为 sshConnect → mountWorkspace → runClaude 三阶段，远程命令以 cd /workspace 为前缀，defer 链保证正确清理顺序</done>
</task>

<task type="auto">
  <name>Task 2: 更新 main.go 传递当前工作目录</name>
  <files>cmd/cloud-claude/main.go</files>
  <read_first>
    - cmd/cloud-claude/main.go（当前完整实现，177 行，确认 ConnectAndRunClaude 调用位置在 line 167）
    - internal/cloudclaude/ssh.go（Task 1 重构后的新签名，确认第三个参数为 cwd string）
  </read_first>
  <action>
修改 `cmd/cloud-claude/main.go` 的 `runRoot` 函数，在调用 `ConnectAndRunClaude` 之前获取当前工作目录并传递。

**Step 1: 在 sshCfg 定义之后、ConnectAndRunClaude 调用之前，添加 CWD 获取：**

```go
cwd, err := os.Getwd()
if err != nil {
    fmt.Fprintln(os.Stderr, "错误: 无法获取当前工作目录: "+err.Error())
    os.Exit(exitInternalError)
}
```

**Step 2: 更新 ConnectAndRunClaude 调用，传入 cwd：**

旧调用：`exitCode, err := cloudclaude.ConnectAndRunClaude(sshCfg, args)`
新调用：`exitCode, err := cloudclaude.ConnectAndRunClaude(sshCfg, args, cwd)`

**Step 3: 更新用户进度提示**

将 line 158 的 `fmt.Println("\r正在进入 Claude Code 会话...")` 改为 `fmt.Println("\r正在映射工作目录并进入 Claude Code 会话...")`，告知用户连接后会先做目录映射。

**不改动的部分：**
- runInit 函数不变
- 错误处理逻辑不变（ConnectAndRunClaude 返回的 error 已包含目录映射失败信息）
- 退出码处理逻辑不变
- import 列表不变（os 已导入）
  </action>
  <verify>
    <automated>cd . && go build ./cmd/cloud-claude/...</automated>
  </verify>
  <acceptance_criteria>
    - main.go 包含 `os.Getwd()`
    - main.go 包含 `cloudclaude.ConnectAndRunClaude(sshCfg, args, cwd)`
    - main.go 包含 `无法获取当前工作目录`（CWD 获取失败的错误提示）
    - main.go 包含 `正在映射工作目录`（更新后的进度提示）
    - `go build ./cmd/cloud-claude/...` 退出码为 0
  </acceptance_criteria>
  <done>main.go 通过 os.Getwd() 获取当前目录并传递给 ConnectAndRunClaude，进度提示已更新</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI 进程 ↔ 容器（SSH session channel） | claude PTY session 和 sshfs SFTP session 共享同一 SSH 连接 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05 | T (Tampering) | ssh.go remoteCmd | mitigate | 远程命令使用 `shellescape.QuoteCommand` 转义（Phase 26 T-26-01 沿用），`cd /workspace` 为硬编码常量路径，不含用户可控输入 |
| T-27-06 | R (Repudiation) | main.go CWD | accept | CWD 是操作系统状态，用户运行 cloud-claude 时所在目录决定映射内容。无需额外审计日志（单用户 CLI 工具） |
</threat_model>

<verification>
1. `go build ./...` — 全项目编译通过
2. `go vet ./internal/cloudclaude/... ./cmd/cloud-claude/...` — 无警告
3. `go test ./internal/cloudclaude/ -run TestWaitForMount -v` — 测试仍通过
4. `grep "func sshConnect" internal/cloudclaude/ssh.go` — 存在
5. `grep "mountWorkspace" internal/cloudclaude/ssh.go` — 存在
6. `grep "cd /workspace" internal/cloudclaude/ssh.go` — 存在
7. `grep "os.Getwd" cmd/cloud-claude/main.go` — 存在
</verification>

<success_criteria>
ConnectAndRunClaude 拆分为 sshConnect → mountWorkspace → runClaude 三阶段；claude 远程命令以 cd /workspace 为前缀；main.go 传递 os.Getwd() 结果；全项目编译通过；waitForMount 测试通过。
</success_criteria>

<output>
After completion, create `.planning/phases/27-session/27-02-SUMMARY.md`
</output>
