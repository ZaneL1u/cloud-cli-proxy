---
phase: quick-260504-2n4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/admin/src/components/hosts/create-host-dialog.tsx
  - web/admin/src/components/hosts/mount-manager.tsx
autonomous: true
requirements:
  - QUICK-260504-2n4-01
must_haves:
  truths:
    - "在创建主机对话框（create-host-dialog）中添加超长挂载路径（如 /Users/aaron/Documents/Claude）后，对话框宽度保持不变，路径以省略号截断显示"
    - "在主机详情挂载管理（mount-manager）中已配置的超长源路径或目标路径不会撑破卡片或外层容器，超出部分以省略号截断"
    - "鼠标悬停在被截断的路径上时，可以通过原生 title 属性看到完整路径"
    - "删除按钮（X）在超长路径场景下仍稳定停在行尾右侧，可点击"
    - "pnpm --filter @cloud-cli-proxy/admin run typecheck 通过"
  artifacts:
    - path: "web/admin/src/components/hosts/create-host-dialog.tsx"
      provides: "已添加挂载条目展示行的 flex 布局修复"
      contains: "flex-1 min-w-0 truncate font-mono"
    - path: "web/admin/src/components/hosts/mount-manager.tsx"
      provides: "目标路径行追加 truncate，确保不溢出"
      contains: "truncate"
  key_links:
    - from: "create-host-dialog.tsx 第 245-260 行 hostMounts.map 渲染"
      to: "Tailwind 4 truncate 生效条件"
      via: "flex 子项加 min-w-0 解除默认 min-width:auto"
      pattern: "flex-1 min-w-0 truncate"
    - from: "mount-manager.tsx 第 67-108 行 localMounts.map 渲染"
      to: "目标路径 <p> 防溢出"
      via: "为 .text-xs.text-muted-foreground 行追加 truncate"
      pattern: "truncate font-mono text-xs"
---

<objective>
修复挂载路径条目展示行在长路径下撑爆对话框 / 卡片的视觉溢出问题。

Purpose: 用户反馈在创建主机对话框中输入 `/Users/aaron/Documents/Claude` 等长路径并添加后，对话框被横向撑开。根因为 flex 子项缺少 `min-w-0`，Tailwind `truncate`（text-overflow: ellipsis）在 flex 容器中不生效。
Output: 两个组件的 flex/truncate 类名修正；视觉上长路径正常省略号截断，hover 显示完整 title。
</objective>

<execution_context>
@/workspace/Desktop/cloud-cli-proxy/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/Desktop/cloud-cli-proxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@web/admin/src/components/hosts/create-host-dialog.tsx
@web/admin/src/components/hosts/mount-manager.tsx

<interfaces>
<!-- 现状代码片段，执行者直接基于此修改，无需再次探索。 -->

create-host-dialog.tsx 第 245-260 行（受影响代码）:
```tsx
{hostMounts.map((m, i) => (
  <div key={i} className="flex items-center gap-2 text-sm">
    <span className="truncate font-mono" title={m.source}>{m.source}</span>
    <span className="text-muted-foreground">-&gt;</span>
    <span className="truncate font-mono" title={m.target}>{m.target}</span>
    <Button
      type="button"
      variant="ghost"
      size="sm"
      className="ml-auto h-6 w-6 p-0"
      onClick={() => setHostMounts(hostMounts.filter((_, j) => j !== i))}
    >
      <X className="h-3 w-3" />
    </Button>
  </div>
))}
```

mount-manager.tsx 第 67-108 行（受影响代码）:
```tsx
<ul className="space-y-3">
  {localMounts.map((m, i) => (
    <li
      key={i}
      className="flex items-center justify-between gap-3 rounded-xl border border-border/80 bg-card px-4 py-3.5 shadow-sm"
    >
      <div className="min-w-0 flex-1">
        <p className="truncate font-mono text-sm leading-tight">
          {m.source}
        </p>
        {m.source !== m.target && (
          <p className="mt-1 font-mono text-xs text-muted-foreground">
            → {m.target}
          </p>
        )}
      </div>
      ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 create-host-dialog 已添加挂载条目展示行溢出</name>
  <files>web/admin/src/components/hosts/create-host-dialog.tsx</files>
  <action>
    定位第 245-260 行 `hostMounts.map((m, i) => ...)` 块。

    将两处 `<span>` 的 className 从：
    ```
    className="truncate font-mono"
    ```
    改为：
    ```
    className="flex-1 min-w-0 truncate font-mono"
    ```
    （即源路径 span 与目标路径 span 都加 `flex-1 min-w-0`，两者各占剩余空间一半）

    保持其余结构不变：
    - 中间 `<span className="text-muted-foreground">-&gt;</span>` 不变（无需 flex-1）
    - 删除按钮 `className="ml-auto h-6 w-6 p-0"` 保持不变（`ml-auto` 在两侧都已是 flex-1 的情况下仍稳定停在右侧）
    - `title={m.source}` / `title={m.target}` 保留，提供 hover 完整路径

    根因说明（不写入代码注释，仅供执行者理解）：flex 子项默认 `min-width: auto`，导致内容会把容器撑开；Tailwind `truncate` 依赖父级宽度约束才能生效。加 `min-w-0` 让子项可以收缩到 0，配合 `flex-1` 让两个路径 span 平均瓜分行内剩余宽度，`truncate` 即可在超长时正确截断。
  </action>
  <verify>
    <automated>cd web/admin && pnpm run typecheck</automated>
    人工目视：在创建主机对话框输入 `/Users/aaron/Documents/Claude` -> `/Users/aaron/Documents/Claude` 并添加，对话框宽度不变，路径出现省略号，hover 显示完整路径，删除按钮可点。
  </verify>
  <done>
    第 247 行与第 249 行两个 span 的 className 各为 `flex-1 min-w-0 truncate font-mono`；typecheck 通过；人工验证长路径不再溢出。
  </done>
</task>

<task type="auto">
  <name>Task 2: 修复 mount-manager 目标路径行潜在溢出</name>
  <files>web/admin/src/components/hosts/mount-manager.tsx</files>
  <action>
    定位第 67-108 行 `localMounts.map((m, i) => ...)` 块。

    现状：
    - 外层 `<div className="min-w-0 flex-1">` 已正确（源路径 `<p className="truncate font-mono text-sm leading-tight">` 的 truncate 已生效）。
    - 但目标路径行 `<p className="mt-1 font-mono text-xs text-muted-foreground">` 缺 `truncate`，超长目标路径仍会撑爆。

    修改：将目标路径 `<p>` 的 className 从：
    ```
    className="mt-1 font-mono text-xs text-muted-foreground"
    ```
    改为：
    ```
    className="mt-1 truncate font-mono text-xs text-muted-foreground"
    ```

    同时为该 `<p>` 追加 `title={m.target}` 属性，确保被截断时 hover 仍可看到完整路径，与源路径行的可访问性保持对称。建议同时为源路径 `<p>` 也加 `title={m.source}`（如尚未存在），统一两行 hover 行为。

    保持其余结构不变：外层 `<li>` 的 flex 布局、`<div className="min-w-0 flex-1">`、删除按钮 `shrink-0` 全部保留。
  </action>
  <verify>
    <automated>cd web/admin && pnpm run typecheck</automated>
    人工目视：在主机详情页挂载管理中查看一条 source/target 不同且均超长（≥60 字符）的挂载条目，源与目标行均省略号截断、卡片宽度不变，hover 任一行均可看到完整路径。
  </verify>
  <done>
    第 74-80 行两个 `<p>` 均带 `truncate` 与 `title` 属性；typecheck 通过；人工验证长路径在卡片内省略号显示。
  </done>
</task>

</tasks>

<verification>
- `cd web/admin && pnpm run typecheck` 全 PASS（无 TS 错误新增）
- 启动 admin 前端 (`pnpm --filter @cloud-cli-proxy/admin run dev` 或同等命令)，目视两个场景：
  1. 创建主机对话框：添加 `/Users/aaron/Documents/Claude` -> `/Users/aaron/Documents/Claude` 等长路径，对话框不被撑开
  2. 主机详情挂载管理：已存在的长路径条目源/目标行均正确省略，卡片宽度稳定
- hover 截断行可见原生 tooltip 显示完整路径
- 删除按钮在长路径下仍可点击且位置稳定
</verification>

<success_criteria>
- [ ] create-host-dialog.tsx 两 span 均含 `flex-1 min-w-0 truncate font-mono`
- [ ] mount-manager.tsx 目标路径 `<p>` 含 `truncate` 与 `title` 属性
- [ ] pnpm typecheck PASS
- [ ] 人工目视长路径场景均不再撑爆容器
- [ ] git commit 完成（Conventional Commits, 中文 body 说明根因）
</success_criteria>

<output>
执行完成后创建 `.planning/quick/260504-2n4-fix-mount-path-overflow/260504-2n4-SUMMARY.md`，记录：
- 改动 diff 摘要（两个 className 改动 + 一个 title 追加）
- 根因（flex min-width:auto + truncate 协同）
- 验证结果（typecheck 输出 + 目视确认）
- commit hash
</output>
