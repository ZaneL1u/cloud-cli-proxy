---
phase: quick-260507-3zk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/admin/src/index.css
  - web/admin/src/components/ui/status-dot.tsx
  - web/admin/src/components/ui/table-skeleton.tsx
  - web/admin/src/components/ui/table.tsx
  - web/admin/src/routes/_dashboard/hosts/index.tsx
  - web/admin/src/routes/_dashboard/users/index.tsx
  - web/admin/src/routes/_dashboard/egress-ips/index.tsx
autonomous: true
requirements:
  - QUICK-260507-3zk-A
must_haves:
  truths:
    - "三页（hosts / users / egress-ips）的状态色全部走 success / warning / info / danger / muted 语义 token，无硬编码 text-green-600 / text-yellow-600 / text-red-500 / text-blue-500"
    - "三页 loading 态显示差异化骨架（不同列宽 + pill / muted 区分），不再是均匀色块"
    - "三页 empty 态在 DataTableShell 卡片中央，不再被嵌进 TableCell 里被表格 border 切割"
    - "全站表头有浅灰底（bg-muted/40）+ 小字 uppercase + tracking-wide，扫视性增强"
    - "全站行高放宽约 20%（h-10→h-11 表头；p-2→px-3 py-3.5 单元格）"
    - "hosts 运行中 = 绿点，启动中 = 蓝点（pulse），失败 = 红点；任务进行中色调改 text-info（避免与 success 撞车）"
    - "users 活跃 = 绿点，已过期 = 红点，已禁用 = 灰点；到期/创建时间列加 tabular-nums"
    - "egress 正常 = 绿点，部分异常 = 琥珀点，异常 = 红点"
    - "StatusDot pulse 在 prefers-reduced-motion 下自动停止（motion-reduce:animate-none）"
    - "cd web/admin && npm run typecheck 通过，零类型错误"
  artifacts:
    - path: "web/admin/src/index.css"
      provides: "新增 success / warning / info 语义 token（@theme + :root 双段）"
      contains: "--color-success"
    - path: "web/admin/src/components/ui/status-dot.tsx"
      provides: "StatusDot 组件，6 个 variant + pulse + size"
      exports: ["StatusDot"]
    - path: "web/admin/src/components/ui/table-skeleton.tsx"
      provides: "TableSkeleton 组件，按列配置宽度 / pill / muted / align"
      exports: ["TableSkeleton"]
    - path: "web/admin/src/components/ui/table.tsx"
      provides: "Table primitive 表头加重 + 行高放宽 + hover accent"
      contains: "bg-muted/40"
    - path: "web/admin/src/routes/_dashboard/hosts/index.tsx"
      provides: "hosts 列表页 StatusDot + TableSkeleton + 空态独立 + 状态色 token 化"
    - path: "web/admin/src/routes/_dashboard/users/index.tsx"
      provides: "users 列表页 StatusDot + TableSkeleton + 空态独立 + tabular-nums"
    - path: "web/admin/src/routes/_dashboard/egress-ips/index.tsx"
      provides: "egress 列表页 StatusDot + TableSkeleton + 空态独立 + StatusCell token 化"
  key_links:
    - from: "web/admin/src/components/ui/status-dot.tsx"
      to: "web/admin/src/index.css"
      via: "Tailwind utility bg-success / bg-warning / bg-info / bg-destructive 引用 @theme token"
      pattern: "bg-success|bg-warning|bg-info"
    - from: "三个列表页 index.tsx"
      to: "web/admin/src/components/ui/status-dot.tsx"
      via: "import { StatusDot }"
      pattern: "import.*StatusDot.*status-dot"
    - from: "三个列表页 index.tsx"
      to: "web/admin/src/components/ui/table-skeleton.tsx"
      via: "import { TableSkeleton } + isLoading 分支渲染"
      pattern: "TableSkeleton"
---

<objective>
把 .planning/quick/260507-3o9-a/HANDOFF.md 第 4 节锁定的 6 项原子改动机械落地，完成 hosts / users / egress-ips 三个列表页的 A 档视觉收敛。

Purpose: 解决三页状态色硬编码、表头无对比、行高过紧、loading 像色块阵列、empty 态被表格切割等长期视觉债，统一到 shadcn 风格 + 语义 token。

Output: 新增 2 个 UI 组件（StatusDot / TableSkeleton）、扩展 3 个语义 token、改造 1 个 Table primitive、改造 3 个列表页路由文件，全部走 6 个独立 commit。
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/quick/260507-3o9-a/HANDOFF.md
@CLAUDE.md
@web/admin/src/index.css
@web/admin/src/components/ui/table.tsx
@web/admin/src/routes/_dashboard/hosts/index.tsx
@web/admin/src/routes/_dashboard/users/index.tsx
@web/admin/src/routes/_dashboard/egress-ips/index.tsx

<authoritative_source>
HANDOFF.md 第 4 节是本任务唯一权威源，所有 before/after 源码、行号、字面量均以 HANDOFF 为准。本 PLAN 仅作路标和提交编排，不重复贴代码。执行 Claude 务必逐项打开 HANDOFF 第 4 节对应小节读源码 diff 后再落地。
</authoritative_source>

<conventions>
- 沟通语言：所有 commit message、注释、控制台输出全部中文（CLAUDE.md 强约束）。
- 不引入新 npm 依赖、不动后端、不扩散到 events / tasks / 详情页（HANDOFF 第 8.1 仅要求"扫一眼无回归"，不算独立 task）。
- 每个 task 独立 commit，commit message 严格使用 HANDOFF 给出的字面量。
- task 5 / 6 各为单一 task（HANDOFF 建议合 1 个 commit，本 plan 采纳此建议）。
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 success / warning / info 语义 token</name>
  <files>web/admin/src/index.css</files>
  <action>
    按 HANDOFF.md 第 4 节「改动 1」执行：
    - 在 `@theme {` 块内追加 6 行：--color-success / --color-success-foreground / --color-warning / --color-warning-foreground / --color-info / --color-info-foreground，全部映射到 var(--xxx)
    - 在 `:root {` 块内追加 6 行 oklch 字面量（success=oklch(0.62 0.17 145)、warning=oklch(0.72 0.17 75)、info=oklch(0.58 0.16 220)，对应 -foreground 见 HANDOFF）
    - 紫蓝主色 --primary 字面量不动
    完成后独立提交：feat(admin): add success/warning/info semantic tokens
  </action>
  <verify>
    <automated>cd web/admin && grep -q "color-success" src/index.css && grep -q "oklch(0.62 0.17 145)" src/index.css</automated>
  </verify>
  <done>index.css 同时包含 @theme 段 6 行 var 映射与 :root 段 6 行 oklch 字面量；commit 已落地</done>
</task>

<task type="auto">
  <name>Task 2: 新建 StatusDot 组件</name>
  <files>web/admin/src/components/ui/status-dot.tsx</files>
  <action>
    按 HANDOFF.md 第 4 节「改动 2」直接落地完整代码（约 50 行）：
    - 6 个 variant: success / warning / info / danger / muted / loading
    - props: variant 必传、pulse 默认 false、size "sm"|"md" 默认 "sm"、className
    - sm = h-2 w-2，md = h-2.5 w-2.5
    - variantClass 表把 success/warning/info 映射到 bg-success / bg-warning / bg-info；danger→bg-destructive；muted→bg-muted-foreground/50；loading→bg-info
    - pulse 分支用 motion-safe:animate-ping motion-reduce:animate-none（reduced motion 自动停）
    - role="presentation"，外层 inline-flex shrink-0
    依赖 @/lib/utils 的 cn（已存在）。
    完成后独立提交：feat(admin): add StatusDot component using semantic tokens
  </action>
  <verify>
    <automated>test -f web/admin/src/components/ui/status-dot.tsx && grep -q "motion-reduce:animate-none" web/admin/src/components/ui/status-dot.tsx && grep -q "bg-success" web/admin/src/components/ui/status-dot.tsx</automated>
  </verify>
  <done>status-dot.tsx 存在；包含 6 个 variant + pulse + reduced motion 兼容；commit 已落地</done>
</task>

<task type="auto">
  <name>Task 3: 表头加重 + 行高放宽（table.tsx）</name>
  <files>web/admin/src/components/ui/table.tsx</files>
  <action>
    按 HANDOFF.md 第 4 节「改动 3」三段同时改：
    - 3a TableHead（约第 66–77 行）：h-10 px-2 ... text-foreground → h-11 bg-muted/40 px-3 ... text-xs font-medium uppercase tracking-wide ... text-muted-foreground
    - 3b TableCell（约第 79–90 行）：p-2 → px-3 py-3.5
    - 3c TableRow（约第 53–64 行）：hover:bg-muted/50 → hover:bg-accent/40
    其它 className 字面量、cn() 顺序、checkbox 选择器保持原样不动。
    注意：本改动全站影响（events / tasks / hosts/$hostId / users/$userId 详情页都用 Table），落地后随手切到这些页面眼看一眼有没有溢出 / 截断回归（HANDOFF 第 8.1 仅要求"扫一眼"，不算独立 task）。
    完成后独立提交：style(admin): emphasize table headers and relax row density
  </action>
  <verify>
    <automated>grep -q "bg-muted/40 px-3" web/admin/src/components/ui/table.tsx && grep -q "px-3 py-3.5" web/admin/src/components/ui/table.tsx && grep -q "hover:bg-accent/40" web/admin/src/components/ui/table.tsx</automated>
  </verify>
  <done>table.tsx 三处 className 已替换为新值；commit 已落地</done>
</task>

<task type="auto">
  <name>Task 4: 新建 TableSkeleton 组件</name>
  <files>web/admin/src/components/ui/table-skeleton.tsx</files>
  <action>
    按 HANDOFF.md 第 4 节「改动 4」直接落地完整代码（约 50 行）：
    - 导出接口 TableSkeletonColumn { width: string; pill?: boolean; muted?: boolean; align?: "left"|"right"|"center" }
    - 导出接口 TableSkeletonProps { columns: TableSkeletonColumn[]; rows?: number 默认 4 }
    - 渲染：返回 TableBody，rows × columns 二维循环
    - 内层 div: animate-pulse + col.width + (pill ? "h-5 rounded-full" : "h-4 rounded") + (muted ? "bg-muted/60" : "bg-muted") + align 的 ml-auto / mx-auto
    - 外层 TableCell 用 align 决定 text-right / text-center
    依赖 cn 与 TableBody/TableCell/TableRow（来自 @/components/ui/table）。
    完成后独立提交：feat(admin): add TableSkeleton component for differentiated loading state
  </action>
  <verify>
    <automated>test -f web/admin/src/components/ui/table-skeleton.tsx && grep -q "TableSkeleton" web/admin/src/components/ui/table-skeleton.tsx && grep -q "animate-pulse" web/admin/src/components/ui/table-skeleton.tsx && grep -q "rounded-full" web/admin/src/components/ui/table-skeleton.tsx</automated>
  </verify>
  <done>table-skeleton.tsx 存在；包含 columns 配置、rounded-full pill 分支、animate-pulse；commit 已落地</done>
</task>

<task type="auto">
  <name>Task 5: 三页空态独立 + 启用 TableSkeleton</name>
  <files>web/admin/src/routes/_dashboard/hosts/index.tsx, web/admin/src/routes/_dashboard/users/index.tsx, web/admin/src/routes/_dashboard/egress-ips/index.tsx</files>
  <action>
    按 HANDOFF.md 第 4 节「改动 5」对三页同时套用通用模式：把原来 `<DataTableShell><Table><TableHeader/><TableBody>{isLoading ? skeleton-rows : list.length===0 ? <TableRow><TableCell colSpan/><EmptyState/></TableCell></TableRow> : list.map(...)}</TableBody></Table></DataTableShell>` 拆三态 —
    - isLoading 分支：DataTableShell > Table > TableHeader + TableSkeleton
    - 空态分支：DataTableShell > EmptyState（不再嵌进 TableCell colSpan）
    - 数据分支：DataTableShell > Table > TableHeader > TableBody.map

    三页的 TableSkeleton columns 严格按 HANDOFF 第 4 节给出：
    - 5a hosts: 6 列 [w-28, w-20, w-24, w-20+pill, w-28+muted, w-12+align:right]
    - 5b users: 5 列 [w-24, w-16+pill, w-28+muted, w-32+muted, w-8+align:right]
    - 5c egress-ips: 5 列 [w-24, w-48, w-28, w-20+pill, w-8+align:right]

    其它行为（loading 旁路、错误旁路、行内回调）保持现状。
    三页合并 1 个 commit（HANDOFF 建议）：style(admin): elevate empty state and use TableSkeleton across list pages
  </action>
  <verify>
    <automated>grep -q "TableSkeleton" web/admin/src/routes/_dashboard/hosts/index.tsx && grep -q "TableSkeleton" web/admin/src/routes/_dashboard/users/index.tsx && grep -q "TableSkeleton" web/admin/src/routes/_dashboard/egress-ips/index.tsx</automated>
  </verify>
  <done>三页 isLoading 分支均渲染 TableSkeleton；空态独立放在 DataTableShell 顶层不再被 TableCell 包裹；commit 已落地</done>
</task>

<task type="auto">
  <name>Task 6: 三页状态色统一走 token + StatusDot</name>
  <files>web/admin/src/routes/_dashboard/hosts/index.tsx, web/admin/src/routes/_dashboard/users/index.tsx, web/admin/src/routes/_dashboard/egress-ips/index.tsx</files>
  <action>
    按 HANDOFF.md 第 4 节「改动 6」执行三页改造：

    6a hosts/index.tsx
      - 6a.1 getHostStatus() 返回类型 { type, label, variant } → { type:"dot", label, tone: "success"|"info"|"danger"|"muted" }；映射：failed→danger / pending→muted / running→success / stopped→muted / 默认 muted
      - 6a.2 状态列把 <Badge variant={status.variant}>...</Badge> 改成 <span className="inline-flex items-center gap-2 text-sm"><StatusDot variant={status.tone}/>{status.label}</span>；任务进行中分支文字色由 text-primary → text-info，用 <StatusDot variant="info" pulse />；Loader2 与 dot 二选一（保留 dot+pulse 更干净）
      - 6a.3 行内 Play / Square / RotateCcw 三个 ghost button：text-green-600 → text-success；text-red-500 → text-destructive；text-blue-500 → text-info
      - 6a.4 文件顶部追加 import { StatusDot } from "@/components/ui/status-dot"；如果 Badge 不再使用则移除其 import

    6b users/index.tsx
      - 6b.1 状态 Badge → StatusDot：active→success / expired→danger / disabled→muted；外层 <span className="inline-flex items-center gap-2 text-sm">
      - 6b.2 到期时间列与创建时间列 className 追加 tabular-nums
      - 6b.3 import StatusDot；如果 Badge 不再使用则移除其 import
      - 不引入"剩 ≤7 天 = warning"逻辑（A 档不做）

    6c egress-ips/index.tsx
      - 6c.1 StatusCell（约第 406–499 行）所有硬编码颜色统一 token 化：
        passed: text-green-600 → text-success，<Check> → <StatusDot variant="success" />，文案"正常"
        partial: text-yellow-600 → text-warning，<X> → <StatusDot variant="warning" />，文案"部分异常"
        failed: text-destructive 保留，<X> → <StatusDot variant="danger" />
        disabled: 改为 <StatusDot variant="muted" /> + "已禁用"（HANDOFF 建议改 dot 求全页一致）
        isTesting: 保留 Loader2，配色改 text-info（避免双重动画，不前置 dot）
        待测试（约第 433–438 行）：<Minus> → <StatusDot variant="muted" />，text-muted-foreground 保留
      - 6c.2 实际 IP 列 RefreshCw 小按钮（约第 252–259 行）保持中性 text-muted-foreground hover:text-foreground，不 token 化
      - 6c.3 import StatusDot；移除不再使用的 Check / X / Minus import

    全部完成后必须跑：cd web/admin && npm run typecheck（收口校验，零类型错误）
    三页合并 1 个 commit：style(admin): unify list page status colors via tokens and StatusDot
  </action>
  <verify>
    <automated>cd web/admin && grep -q "StatusDot" src/routes/_dashboard/hosts/index.tsx && grep -q "StatusDot" src/routes/_dashboard/users/index.tsx && grep -q "StatusDot" src/routes/_dashboard/egress-ips/index.tsx && ! grep -q "text-green-600\|text-yellow-600\|text-red-500\|text-blue-500" src/routes/_dashboard/hosts/index.tsx src/routes/_dashboard/users/index.tsx src/routes/_dashboard/egress-ips/index.tsx && npm run typecheck</automated>
  </verify>
  <done>三页均 import 并使用 StatusDot；三页源码内已无硬编码 text-green-600 / text-yellow-600 / text-red-500 / text-blue-500；npm run typecheck 退出码 0；commit 已落地</done>
</task>

</tasks>

<verification>
- 自动：`cd web/admin && npm run typecheck` 通过（task 6 内置）
- 自动：三页源码 grep 无 text-green-600 / text-yellow-600 / text-red-500 / text-blue-500 残留
- 自动：三页 grep 命中 StatusDot 与 TableSkeleton
- 人工（用户在浏览器 :5173 自检 HANDOFF 第 5 节清单）：
  - 三页 loading 显示差异化骨架（不同列宽 + pill / muted）
  - 三页 empty 在卡片中央，不被表格 border 切割
  - hosts 运行中=绿点 / 启动中=蓝点 pulse / 失败=红点
  - users 活跃=绿点 / 已过期=红点 / 已禁用=灰点
  - egress 正常=绿点 / 部分异常=琥珀点 / 异常=红点
  - 表头浅灰底 + 小字 uppercase；行高比之前明显宽松；hover 行浅 accent
  - 系统 reduced motion 下 StatusDot pulse 停止
- 人工（HANDOFF 第 8.1）：扫一眼 events / tasks / hosts/$hostId / users/$userId 详情页 Table，确认行高放宽未引入溢出 / 截断回归
</verification>

<success_criteria>
- [ ] 6 个 commit 全部就绪，commit message 完全匹配 HANDOFF 第 4 节给出的字面量
- [ ] 三页状态色 100% token 化（无 text-green-600 / text-yellow-600 / text-red-500 / text-blue-500 残留）
- [ ] 三页 loading 走 TableSkeleton，empty 走 DataTableShell 顶层 EmptyState
- [ ] StatusDot pulse 在 prefers-reduced-motion 下自动停止
- [ ] cd web/admin && npm run typecheck 退出码 0
- [ ] 无新增 npm 依赖（package.json 仅 web/admin 现有 diff，无新条目）
- [ ] 后端 / events / tasks 列表页未被改动
</success_criteria>

<output>
完成后创建 `.planning/quick/260507-3zk-a/260507-3zk-SUMMARY.md`，包含：
- 6 个 commit 的 hash 与 message
- typecheck 输出截断（PASS 即可）
- 是否在浏览器 reduced motion 模式下验证 pulse 行为
- HANDOFF 第 8.1「扫一眼」其它 Table 页面的回归判断（无 / 有，若有列出）
</output>
